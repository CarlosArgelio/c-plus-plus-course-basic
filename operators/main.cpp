#include <iostream>

using namespace std;

int main() {

    int a = 2;
    int b = 3;

    int sum = a + b;
    int multiply = a * b;
    bool greater_than = a > b;
    bool smaller_than = a < b;

    cout << sum << endl;
    cout << multiply << endl;
    cout << greater_than << endl;
    cout << smaller_than << endl;

}